import { styled } from "@mui/material";

export const PokemonTeamWrapper = styled("div")({
  display: "flex",
  flexirection: "column",
  alignItems: "start",
  justifyContent: "center",
  height: "100%",
  maxWidth: "25rem",
  margin: "1rem",
});
